#include <stdio.h>

int setBit(int num, int pos) {
    return num | (1 << pos);
}

int main() {
    int n, p, res;
    printf("Num: ");
    scanf("%d", &n);
    printf("Pos: ");
    scanf("%d", &p);
    res = setBit(n, p);
    printf("Orig: %d, Mod: %d\n", n, res);
    return 0;
}


